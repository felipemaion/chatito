version: "2"
linters:
  default: standard
  settings:
    errcheck:
      # Writes to stdout/stderr in the CLI: nothing useful to do on failure.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
