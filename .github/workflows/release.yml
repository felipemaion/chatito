name: release

# Em tag v*: builda APK (Android), .dmg (macOS, sem notarização), .zip (Windows) e a imagem
# multi-arch do relay em ghcr.io; anexa os binários a um GitHub Release (repo privado ⇒
# release privado). Distribuição familiar: sideload/abrir com botão direito/aviso SmartScreen.
on:
  push:
    tags: ['v*']

env:
  FLUTTER_VERSION: 3.47.2
  IMAGE: ghcr.io/felipemaion/piriquito-relay

permissions:
  contents: read

jobs:
  android:
    runs-on: ubuntu-latest
    defaults: { run: { working-directory: app } }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '17' }
      - uses: subosito/flutter-action@v2
        with: { channel: stable, flutter-version: '${{ env.FLUTTER_VERSION }}', cache: true }
      - run: flutter pub get
      - name: google-services.json (push FCM — opcional)
        env:
          GS: ${{ secrets.GOOGLE_SERVICES_JSON_B64 }}
        run: '[ -n "$GS" ] && printf %s "$GS" | base64 -d > android/app/google-services.json || echo "sem GOOGLE_SERVICES_JSON_B64 — build sem push"'
      - name: Decidir modo de assinatura
        id: mode
        env:
          KS: ${{ secrets.ANDROID_KEYSTORE_B64 }}
          ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KPASS: ${{ secrets.ANDROID_KEY_PASSWORD }}
          SPASS: ${{ secrets.ANDROID_STORE_PASSWORD }}
        run: |
          if [ -n "$KS" ] && [ -n "$ALIAS" ] && [ -n "$KPASS" ] && [ -n "$SPASS" ]; then
            echo "signed=true" >> "$GITHUB_OUTPUT"
          else
            echo "::warning::Secrets ANDROID_* ausentes — gerando APK debug (não instalável sobre um release assinado)."
            echo "signed=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Build APK release (assinado pelo Gradle via android/key.properties)
        if: steps.mode.outputs.signed == 'true'
        env:
          KS: ${{ secrets.ANDROID_KEYSTORE_B64 }}
          ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KPASS: ${{ secrets.ANDROID_KEY_PASSWORD }}
          SPASS: ${{ secrets.ANDROID_STORE_PASSWORD }}
        run: |
          umask 077
          KS_FILE="$RUNNER_TEMP/upload-keystore.jks"
          printf '%s' "$KS" | base64 -d > "$KS_FILE"
          printf 'storeFile=%s\nstorePassword=%s\nkeyAlias=%s\nkeyPassword=%s\n' \
            "$KS_FILE" "$SPASS" "$ALIAS" "$KPASS" > android/key.properties
          flutter build apk --release
          cp build/app/outputs/flutter-apk/app-release.apk "build/piriquito-${GITHUB_REF_NAME}-android.apk"
          rm -f "$KS_FILE" android/key.properties
      - name: Build APK debug (sem keystore)
        if: steps.mode.outputs.signed != 'true'
        run: |
          flutter build apk --debug
          cp build/app/outputs/flutter-apk/app-debug.apk "build/piriquito-${GITHUB_REF_NAME}-android-debug.apk"
      - uses: actions/upload-artifact@v4
        with: { name: android, path: app/build/piriquito-*.apk, if-no-files-found: error }

  macos:
    runs-on: macos-latest
    defaults: { run: { working-directory: app } }
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with: { channel: stable, flutter-version: '${{ env.FLUTTER_VERSION }}', cache: true }
      - run: flutter pub get
      - run: flutter build macos --release
      - name: Empacotar .dmg (sem notarização)
        run: |
          brew install create-dmg
          APP="build/macos/Build/Products/Release/Piriquito.app"
          test -d "$APP"
          create-dmg --volname "Piriquito" --window-size 500 300 --icon-size 100 \
            --icon "Piriquito.app" 125 150 --app-drop-link 375 150 --no-internet-enable \
            "build/piriquito-${GITHUB_REF_NAME}-macos.dmg" "$APP"
      - uses: actions/upload-artifact@v4
        with: { name: macos, path: app/build/piriquito-*.dmg, if-no-files-found: error }

  windows:
    runs-on: windows-latest
    defaults: { run: { working-directory: app } }
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with: { channel: stable, flutter-version: '${{ env.FLUTTER_VERSION }}', cache: true }
      - run: flutter pub get
      - run: flutter build windows --release
      - name: Empacotar .zip
        shell: pwsh
        run: |
          Compress-Archive -Path build/windows/x64/runner/Release/* `
            -DestinationPath "build/piriquito-$env:GITHUB_REF_NAME-windows.zip"
      - uses: actions/upload-artifact@v4
        with: { name: windows, path: app/build/piriquito-*.zip, if-no-files-found: error }

  server-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMAGE }}:${{ github.ref_name }}
            ${{ env.IMAGE }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  release:
    needs: [android, macos, windows, server-image]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with: { path: dist, merge-multiple: true }
      - name: Checksums
        run: cd dist && sha256sum -- * > "piriquito-${GITHUB_REF_NAME}-SHA256SUMS.txt" && cat "piriquito-${GITHUB_REF_NAME}-SHA256SUMS.txt"
      - name: Criar GitHub Release com os binários
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" --title "Piriquito $GITHUB_REF_NAME" --generate-notes \
            --notes "Imagem do servidor: \`$IMAGE:$GITHUB_REF_NAME\` (linux/amd64, linux/arm64). Ver README → Instalação." \
            dist/*
