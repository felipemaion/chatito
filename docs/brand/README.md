# Marca

- `piriquito-logo.svg` — marca em fundo transparente (periquito-de-encontro-amarelo falando).
- `piriquito-icon.svg` — variante para ícone de app (fundo arredondado claro).
- PNGs 1024 px exportados com `rsvg-convert`.
