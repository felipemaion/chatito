# Copiar para /home/<DOMINIO>/secrets/env (0600). Nunca commitar valores reais.
RELAY_ADDR=:8080
RELAY_DATA_DIR=/app/data
RELAY_PUBLIC_URL=https://<DOMINIO>
# FCM HTTP v1 — conteúdo do service-account JSON em base64 (opcional; sem ele, push desliga)
FCM_SERVICE_ACCOUNT_B64=
